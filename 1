<?php
    $a = substr($a, 0, 180); //Обрезка текста до 180 символов

    $arr = explode('', $a); //Преобразование строки в массив с помощью разделителя в виде пробела, чтобы получить отдельные слова

    $arr = array_chunk($arr, count($arr) - 2); //Отделение последних двух слов от остального текста

    $a = implode('', $arr[0]);

    $link = "<a href='$link'>". implode($arr[1]) . "...</a>";

    $b = $a . $link;
    
    /* 
      Если в переменной $a окажется меньше 180 символов, то первая строка вернёт false, поэтому если такое возможно, то стоит сделать проверку на количество символов в тексте
    */
